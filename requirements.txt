# --- API layer ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Retrieval ---
qdrant-client[fastembed]>=1.10        # brings fastembed: BGE dense + BM25 sparse
rapidfuzz>=3.9                        # metadata / entity resolution
numpy>=1.26

# --- LLM (default provider) ---
anthropic>=1.0

# --- Data pipeline ---
httpx>=0.27                           # SEC EDGAR XBRL companyfacts client

# --- Dev / eval ---
pytest>=8.0
pytest-asyncio>=0.23
